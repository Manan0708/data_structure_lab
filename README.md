# data_structure_lab
open repo in website. 
click on code(green button)
copy link https
go to vs code 
open folder in which you want to have the repo
after opening it open terminal and write git clone .
paste the link after writing the above and press enter
type git status to check status of code
4 types of file : untracked :_new files that git does not yet track
modified :-changed file
staged :- file ready to be commited(after using the add command in terminal)
unmodified:- unchanged(after commiting)
type git add to add  or stage a file(git add . file name  for all files in that folder)
type giit commit -m"some message"(yah this is required
